# Push Current Branch

Push the current branch to remote and set up tracking.

```bash
git push -u origin $(git branch --show-current)
```

Use this from within a worktree to share your work with others or continue from another machine.
